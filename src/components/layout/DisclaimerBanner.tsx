export function DisclaimerBanner() {
  return (
    <div
      className="safe-x fixed inset-x-0 bottom-0 z-50 flex min-h-9 items-center justify-center border-t border-white/8 bg-[#0c0e0b]/95 px-3 py-1 text-center text-[0.68rem] font-semibold tracking-wide text-[var(--text-secondary)] backdrop-blur"
      role="note"
    >
      Simulação não oficial. Nenhum voto real é registrado.
    </div>
  );
}
